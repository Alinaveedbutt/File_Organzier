This Python script organizes files in a directory by grouping them based on their file extensions.
It automatically creates folders for each file type and moves the files accordingly.
The user simply provides a folder path as input.
It helps keep directories clean and easy to navigate.
Built using Python’s os and shutil modules.
